[package]
name = "fips-gate"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
description = "Minimal container entrypoint that enforces FIPS mode"

[dev-dependencies]
tempfile = "=3.27.0"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = true
